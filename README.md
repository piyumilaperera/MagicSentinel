# MagicSentinel
A file authenticity checker that opens a file in binary format, reads its magic number (file signature), and compares it against the file's extension to detect whether the true file type matches the declared extension, flagging any potential spoofing or disguised files.
